SBChroot is a tool that simplifies and automates the creates of 
`full install' changeroots for Slackware. The script assumes the 
presence of a mirror on the local system from which it can install.

