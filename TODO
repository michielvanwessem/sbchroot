TODO:
- chroot_create - build in a check to see if the directory actually exist.
- multi OS suport (think slamd64)

Research how usefull this is:
# mknod /chroot/slack10.1_base01/dev/null    c  1 3
# mknod /chroot/slack10.1_base01/dev/random  c  1 8
# mknod /chroot/slack10.1_base01/dev/urandom c  1 9

# cat /etc/hosts > /chroot/slack10.1_base01/etc/hosts
# cat /etc/resolv.conf > /chroot/slack10.1_base01/etc/resolv.conf
# cat /etc/localtime > /chroot/slack10.1_base01/etc/localtime
